#include<stdio.h>
/*
 * main - program that print alphabet in lowercase followed by newline
 * Return: 0
 */
int main(void)
{
	int a;

	a = 'a';
	while (a <= 'z')
	{
		if (a == e && a == q)
			continue;
		putchar(a);
		a++;
	}
	putchar('\n');
	return (0);
}
