#include "main.h"
/**
 * _isupper - it checks upper letter
 * @x: num to be checked
 * Return: 1 for upper else 0
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}
