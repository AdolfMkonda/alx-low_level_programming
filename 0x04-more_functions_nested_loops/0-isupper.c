#include "main.h"
/**
 * _isupper - it checks upper letter
 * @c: char to be checked
 * Return: 1 for upper else 0
 */
int _isupper(int )
{
	if (x >= 'A' && x <= 'Z')
	{
		return (1);
	}
	return (0);
}
